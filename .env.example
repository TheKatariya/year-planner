# Supabase — project "Member Tracker" (rvkhhlhtzweosrzhffct).
# The planner_* tables live here alongside the other BFT tables.
SUPABASE_URL=https://rvkhhlhtzweosrzhffct.supabase.co

# Service role key. Server-side only — never NEXT_PUBLIC_.
# Supabase dashboard → Project Settings → API Keys → service_role.
# The planner tables have RLS on with no policies, so the anon key cannot
# read them; all access goes through server actions using this key.
SUPABASE_SERVICE_ROLE_KEY=
